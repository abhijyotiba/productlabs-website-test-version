/* ==========================================
   HOME.JS - Product Labs
   Homepage-specific JavaScript
   ========================================== */

// Import parallax module
import { initializeParallax } from './modules/parallax.js';

document.addEventListener("DOMContentLoaded", () => {
  
  // Initialize parallax effect
  initializeParallax();
  
  // Note: Product carousel is now initialized inline in index.html
  // with enhanced styling and functionality. The old carousel
  // initialization has been removed to prevent conflicts.
  
});
